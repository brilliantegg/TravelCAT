@model TravelCat.Models.admin

@{
    ViewBag.Title = "Edit1";
}

<h2>Edit1</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>admin</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.admin_id)
        @Html.HiddenFor(model=>model.admin_account)

        <div class="form-group">
            <label class="col-form-label">舊密碼</label>
            <div class="col-md-10">
                <input id="Password1" type="password" name="oldpassword" class="form-control" />
                @Html.ValidationMessageFor(model => model.admin_password, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <label class="col-form-label">新密碼</label>
            <div class="col-md-10">
                <input id="Password1" type="password" name="newpassword" class="form-control" />
                @Html.ValidationMessageFor(model => model.admin_password, "", new { @class = "text-danger" })
            </div>
        </div>

        


      
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="修改" class="btn btn-primary" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
